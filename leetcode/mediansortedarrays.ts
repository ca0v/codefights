function findMedianSortedArrays(nums1: number[], nums2: number[]): number {
    // start from middle of 1st array
    // how many items in 2nd array are left of that middle?
    // how many items in 2nd array are right of that middle?
    // if left and right are equal, then we have the median
    // if left is greater than right, then we slide left half the difference
    // repeat?
    return 0;
}
